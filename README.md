# Aquarius VA Portfolio

## Run locally

1. Open a terminal in this folder.
2. Run `npm install`.
3. Run `npm start`.
4. Open `http://localhost:3000`.

Contact form submissions are saved in `messages.json`.
